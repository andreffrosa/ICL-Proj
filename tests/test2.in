// Test file
	// Single Line Comments Available

// Typecheck Tests

(new 3) := 6;; // 6

let a :int = new 5 in a := !a + 1; !a end;; // 6

let
    x : int = new 10,
    s:int = new 0 in
    while !x > 0 do
        s := !s + !x;
        x := !x - 1
    end;
    !s
end;; // 55

let f :( int,int)int = fun n:int, b:int->
    let
        x : int = new n,
        s : int = new b
    in
        while !x>0 do
            s := !s + !x ; x := !x - 1
        end;
    !s
    end
end
in f(10, 0)+f(100,20)
end;;   //5125